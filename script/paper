#!/usr/bin/env bash

set -eu
set -o pipefail

function main {
    pushd script/..
    lualatex -shell-escape paper.tex
    bibtex paper
    lualatex -shell-escape paper.tex
    lualatex -shell-escape paper.tex
    popd
    exit 0
}

main
